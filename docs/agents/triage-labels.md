# Triage labels

These are descriptive routing labels, not Project Truss lifecycle state.

| Matt role | GitHub label |
|---|---|
| `needs-triage` | `needs-triage` |
| `needs-info` | `needs-info` |
| `ready-for-agent` | `agent-shaped` |
| `ready-for-human` | `ready-for-human` |
| `wontfix` | `wontfix` |

Project Truss preserves existing labels and adds only explicitly requested descriptive labels. It never derives Ready, Claimed, In review, Blocked, or Done from a label.
