interface:
  capabilities: [filesystem.read, filesystem.write, shell, git, github, native.user-input]
  display_name: "Close"
  short_description: "Review, verify, merge, and roll up one resolution set."
  default_prompt: "Use $project-truss:close for shared Standards review, per-ticket Spec review, verified merge, and roll-up."
