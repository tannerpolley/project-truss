## Project Truss domain contract

Layout: `multi-context`.

Use root `CONTEXT-MAP.md` to locate per-context `CONTEXT.md` files and their ADR directories.

Read `CONTEXT-MAP.md` and the nearest applicable context before shaping or implementation. Treat each `CONTEXT.md` as shared glossary and conceptual language; keep hard-to-reverse trade-offs in that context's ADR directory, and do not mirror lifecycle state or implementation plans.
