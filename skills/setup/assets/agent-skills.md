### Project Truss

Repository profile: `{profile}`.

Use `project-truss:setup` once, then `project-truss:start` to begin or resume work. Start applies the profile-specific Truss lifecycle, invokes relevant Matt techniques, and routes each stage until truthful closeout. The other five Project Truss skills intentionally enter a known stage and return to Start.

### Issue tracker

Project Truss uses GitHub-native standalone, root, and leaf issues. See `docs/agents/issue-tracker.md`.{triage}

### Domain docs

Read the configured domain context and ADRs before shaping or implementation. Start records relevant shared terms and invokes domain-modeling or grilling when ambiguity is material. Scientific repositories also apply `docs/agents/scientific-computing.md`. See `docs/agents/domain.md`.
