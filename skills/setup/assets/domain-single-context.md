## Project Truss domain contract

Layout: `single-context`.

Use one root `CONTEXT.md` and root `docs/adr/`.

Read the nearest applicable context before shaping or implementation. Treat `CONTEXT.md` as the shared glossary: keep durable terminology and conceptual invariants there, keep hard-to-reverse trade-offs in `docs/adr/`, and do not mirror lifecycle state or implementation plans.
