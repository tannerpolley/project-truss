interface:
  capabilities: [native.user-input]
  display_name: "Advanced User Input"
  short_description: "Resolve one native Truss decision or blocker."
  default_prompt: "Use $project-truss:advanced-user-input as a native Truss control point, not a Matt method, to ask one bounded question, record the answer in the affected object, and return to Start."
