interface:
  capabilities: [filesystem.read, shell, github, native.user-input]
  display_name: "Start"
  short_description: "Begin, grill, route, and finish Matt-first work."
  default_prompt: "Use $project-truss:start to begin or resume Matt-first work, invoke relevant disciplines, and continue through verified closeout or a named blocker."
