interface:
  capabilities: [filesystem.read, shell, github, native.user-input]
  display_name: "Shape"
  short_description: "Publish Matt-shaped native GitHub outcomes and tickets."
  default_prompt: "Use $project-truss:shape to grill when required, then publish the smallest native GitHub structure."
