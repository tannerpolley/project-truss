interface:
  capabilities: [filesystem.read, shell, github, native.user-input]
  display_name: "Shape"
  short_description: "Publish a profile-specific GitHub contract."
  default_prompt: "Use $project-truss:shape after the scientific or application outcome is confirmed to publish the smallest native GitHub contract, then return to Start."
