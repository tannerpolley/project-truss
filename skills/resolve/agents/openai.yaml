interface:
  capabilities: [filesystem.read, filesystem.write, shell, git, github, native.user-input]
  display_name: "Resolve"
  short_description: "Claim, implement, review, and publish a Ready set."
  default_prompt: "Use $project-truss:resolve to claim a selected Ready leaf or set, apply direct Matt disciplines, publish one pull request, and return to Start."
